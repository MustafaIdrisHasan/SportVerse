# Copy this file to .env and update the values as needed

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=racescope
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Configuration
JWT_SECRET=racescope-super-secret-jwt-key-change-this-in-production-with-at-least-32-characters
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:3000 